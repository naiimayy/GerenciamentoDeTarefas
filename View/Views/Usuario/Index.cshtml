
@{
    ViewBag.Title = "Index";
}

<h3 class=" mt-3">Lista de Usuarios</h3>

<p align="justify" class="mt-4">
    O gerenciamento de acesso de usuários é uma estrutura que facilita a gestão de quem utiliza a rede corporativa.
    Essa tecnologia pode ser usada para registrar e gerenciar o acesso do usuário de forma automatizada.
</p>

<button class="btn btn-primary mt-md-3" data-toggle="modal" data-target="#modalCadastroUsuario"><i class="fas fa-plus"></i> Novo Usuario</button>
<div class="row">
    <div class="col-3 offset-9 mb-3">
        <input type="text" id="campo-pesquisa" placeholder="Busca" class="form-control" value="" />
    </div>
</div>

<table class="table table-striped table-bordered table-hover">
    <thead class="thead-dark">
        <tr>
            <th>Código</th>
            <th>Nome</th>
            <th>Login</th>
            <th>Senha</th>
            <th>Ação</th>
        </tr>
    </thead>
    <tbody id="lista-usuarios"></tbody>
    <tfoot>
        <tr>
            <th>Código</th>
            <th>Nome</th>
            <th>Login</th>
            <th>Senha</th>
            <th>Ação</th>
        </tr>
    </tfoot>
</table>

<!-- Modal -->
<div class="modal fade" id="modalCadastroUsuario" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-xl" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title"
                    id="exampleModalLabel">
                    Cadastro de Usuario
                </h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form id="form" method="POST" action="javascript:void(0)">
                    <div class="row">
                        <div class="col-12">
                            <div class="form-group">
                                <label for="campo-nome">Nome</label>
                                <input type="text" id="campo-nome" name="nome" class="form-control" autocomplete="off" />
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-12">
                            <div class="form-group">
                                <label for="campo-login">Login</label>
                                <input type="text" id="campo-login" name="login" class="form-control" autocomplete="off" />
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-12">
                            <div class="form-group">
                                <label for="campo-senha">Senha</label>
                                <input type="password" id="campo-senha" class="form-control" name="password" autocomplete="off" />
                                <span toggle="#campo-senha" class="fas fa-fw fa-eye icone-campo toggle-senha"></span>
                            </div>
                        </div>
                    </div>

                    <div class="modal-footer">
                        <button type="submit" class="btn btn-success"
                                id="usuario-botao-salvar">
                            <i class="fas fa-save"></i> Salvar
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<script src="~/Scripts/Usuario/Cadastro.js"></script>
